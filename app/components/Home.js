import React, { PropTypes } from 'react';
import Chatbox from './Chatbox';

const Home = () => {
    return (
        <div className="container">
            <Chatbox />
        </div>
    );
};

export default Home;
